# P5 侦察输出  总段数=336  表数=6


## 一、关键词命中段落

[21] <Normal> 　　随着高校办学规模扩大与校园生活节奏加快，校园内遗失与拾得物品的事件频发。传统失物招领依赖布告栏、微信群、QQ 群等渠道，存在信息传播范围有限、检索效率低、易冒领等问题。针对上述问题，本文设计并实现了一个基于 YOLOv8 的校园失物招领智能匹配系统。系统采用前后端分离架构：后端基于 Python FastAPI 与
[79] <Normal> 　　校园失物招领是高校后勤与学生日常管理中高频且长期存在的需求。随着在校生规模扩大、跨校区活动增多，书包、手机、校园卡、钥匙、雨伞等随身物品遗失与拾得事件几乎每天发生。然而，当前多数高校的失物招领仍依赖线下布告栏、食堂公告、以及微信/QQ 群等非结构化渠道：失主需要逐群刷消息、人工比对描述，拾得者缺乏统一发布入口，信息
[80] <Normal> 　　针对以上痛点，本课题研发一套“基于 YOLOv8 的校园失物招领智能匹配系统”。其核心思路是：用计算机视觉自动识别物品类别，把“人找物”的模糊检索转化为“类别+时空+语义”的结构化匹配；用一套防冒领闭环（审计+动态交接码+申诉）保证交接可信。选题兼具工程价值与研究意义：一方面可直接服务校园师生、降低失物找回成本；另
[83] <Normal> 　　在核心技术侧，目标检测算法从两阶段（Faster R-CNN）演进到单阶段（YOLO 系列、SSD），再到无锚框（anchor-free）的 YOLOv8，在精度与速度上取得良好平衡，已广泛用于商品识别、工业质检等场景。2023 年提出的 YOLO-World 进一步支持“开放词汇/零样本”检测，可用文本提示直接识
[85] <Normal> 　　本文主要工作包括：（1）需求与架构设计：面向失主、拾得者、管理员、游客四类角色，梳理功能性与非功能性需求，给出分层架构、数据库 Schema（10 张表）与 RESTful API 规范；（2）进程内视觉打标：将 YOLOv8s 与 YOLO-World 推理以进程内方式嵌入后端，拾得者发布物品时自动完成类别识别，
[88] <Normal> 本课题所属研发领域为智慧校园与计算机视觉交叉方向下的失物招领信息系统。随着高校在校生规模扩大与跨校区活动增多，书包、手机、校园卡、钥匙、雨伞等随身物品遗失与拾得几乎每日发生；而传统失物招领长期依赖线下布告栏、食堂公告与微信/QQ 群等非结构化渠道，存在信息传播范围有限、时效性差、匹配效率低等突出问题，且“凭文字描述难核
[92] <Normal> 　　当前国内外在失物招领系统与物品识别方向的研究可归纳为三类。（1）校园失物招领类微信小程序与自建平台。以“拾到空间”等校园失物招领小程序[5]为代表，提供发布、列表、搜索与消息通知功能，多以关键词/分类浏览为主，物品描述依赖用户手工录入，图像理解能力薄弱，匹配高度依赖用户主动比对，效率有限，构成本系统的需求对照基线。
[95] <Normal> 本文研发设想是构建一套 Web 端“基于 YOLOv8 的校园失物招领智能匹配系统”：以计算机视觉自动识别物品类别，把模糊检索转化为结构化匹配，并以可信交接闭环保证归属可溯源。研发方法采用前后端分离架构：后端基于 FastAPI+SQLAlchemy 2.x[1][7]，将微调 YOLOv8s 与 YOLO-World
[102] <Normal> YOLO（You Only Look Once）是将目标检测建模为单次回归问题的单阶段检测器，以速度快、易部署著称。YOLOv8 由 Ultralytics 于 2023 年发布，采用无锚框（anchor-free）解耦头与 CIoU 损失，在 COCO 数据集上取得精度与推理速度的较好平衡，并提供 n/s/m/l/x
[103] <Normal> 　　YOLO-World 是 2023 年提出的“开放词汇（open-vocabulary）目标检测”模型，基于视觉-语言对齐（如 CLIP 文本编码器）实现零样本检测：用户只需以文本提示（prompt）描述待检类别（如“campus card, student ID”“keychain with keys”“othe
[104] <Normal> FastAPI 是基于 Python asyncio 的现代 Web 框架，以类型注解驱动自动生成 OpenAPI 文档、依赖注入（Depends）管理服务与鉴权、原生支持异步，非常适合构建 JSON 风格、平台无关的后端接口（Web 与小程序共用）。SQLAlchemy 2.x 是 Python 主流 ORM，通过声
[105] <Normal> 　　上述技术中，YOLOv8 与 YOLO-World 构成系统的“视觉大脑”，负责把用户上传的照片转化为结构化类别标签；FastAPI+SQLAlchemy+MySQL+Redis 构成“业务骨架”，负责任务编排、持久化与高并发；Vue3+Element Plus 构成“交互界面”；JWT 与 WebSocket 解
[108] <Normal> 本系统选用 YOLOv8s（small）作为通用物品识别主干：其一，COCO 预训练已覆盖书包、手机、笔记本、水杯、雨伞等校园高频失物类，可直接复用并微调；其二，相较 YOLOv8n（3.16M 参数 / 8.9 GFLOPs），YOLOv8s（11.14M 参数 / 28.7 GFLOPs）容量更大，在本文长尾数据集
[111] <Normal> YOLO-World 是 2023 年提出的开放词汇（Open-Vocabulary）目标检测模型，基于 CLIP 文本编码器实现零样本检测：用户只需以文本提示词描述类别，模型即可识别训练时未出现的新物体，突破了闭集检测器的类别天花板。[10][4]
[112] <Normal> 尽管微调后的 YOLOv8s 已覆盖多数常用类，但“校园卡、钥匙、笔记本”等校园专属物品并不在 COCO 之中。为此，本系统保留 YOLO-World 作为可插拔的零样本兜底：以“campus card / key / notebook”等提示词识别上述专属类，与 YOLOv8s 形成“通用+专属”的双路视觉底座，且同
[118] <Normal> 　　系统功能性需求（节选）包括：FR-1 用户注册/登录（双因子短信验证码）；FR-2 失主发布失物（含类别、地点、时间、描述）；FR-3 拾得者发布拾物（拍照+保管状态二选一，零门槛）；FR-4 系统自动视觉打标；FR-5 反向主动匹配并推送疑似结果；FR-6 失主认领（填写认领理由作为凭证）；FR-7 动态交接码双
[122] <Normal> 　　数据字典（DD）对核心字段给出类型与约束，例如用户表含学号（唯一登录账号）、手机号（双因子）、信誉分（默认 100）；失物/拾物表含类别外键、6 位行政区划码 region_code、状态机字段；审计表含操作类型、目标对象、IP/UA/会话/GPS 与原文明细，并按月分区。
[131] <Normal> 　　用户界面以响应式 Web 呈现：首页/公示栏（脱敏）、发布页（拍照+保管状态）、我的匹配列表（按匹配度降序）、交接确认页（双端扫码）、管理后台（审计时间线）。界面风格统一采用 Element Plus，适配 PC 与手机。
[135] <Normal> 　　后端采用经典分层：routers（薄路由层，仅做鉴权依赖注入、参数校验、调用 service、统一响应包装）、services（业务逻辑编排）、models（SQLAlchemy 声明式模型）、schemas（Pydantic 请求/响应模型）、utils（无状态纯函数，如时间衰减、地点层级、Jaccard 相似度
[143] <Normal> score = 20·(photo⊕category) + 15·qty + 20·color + 10·state + 15·place + 10·keyword + 10·time      (式 3-2，各子项∈[0,1]，权重和=100)
　　归一化：设 W 为失主实际提供维度的权重之和，k = 100 / m
[144] <Normal> 　　评分引擎经历了两轮演进：初版采用“图像相似、类别、外观、特征、时间、地点”六维加权（式 3-1 旧），但外观/特征与文字属性、地点与文字地点存在语义重合，且权重固定未归一化，导致“失主描述越完整得分反而未必越高”。第二轮将冗余维度并入统一的文字语义维度，形成“分类 + 文字 + 时间”的精简结构；最终 v2 版进一
[211] <Normal> 匹配模块（MatchService）是算法核心。给定一条失物 l 与一条拾物 f，匹配度 score 由「分类 + 文字语义 + 时间」七子维度加权得到（v2 评分引擎）：score = 20·(photo⊕category) + 15·qty + 20·color + 10·state + 15·place + 10
[212] <Normal> 候选集先经类别主键过滤，再按归一化后的 total 降序返回；低于阈值（threshold=80，疑似线）的匹配不予推送，但“前 10 条保底 + 所有≥80 疑似全列”的护栏避免漏召回。photo_category、qty、color、state、place、keyword、time 七维打分与归一化系数 norm_
[216] <Normal> 发布模块（PublishService）负责“零门槛发布”：拾得者上传照片与保管状态后，服务在进程内调用 VisionService.predict() 完成视觉打标并写入 found_item，随即触发反向主动匹配——查询同类别、待认领的失物交由 MatchService 打分，生成疑似匹配记录并推送。
[218] <Normal> 在上述防冒领闭环基础上，系统进一步引入数据生命周期与取证管理能力（v7 迭代）：其一，用户可对误发的失物/拾物执行软删除（置 deleted_at，物理记录保留以满足审计与外键约束）；其二，每条物品设失效倒计时（expires_at，用户侧保留 3 个月、管理员侧保留 1 年，完成匹配交接后重置计时），到期对用户侧自动
[224] <Normal> 　　本系统的视觉底座以单路微调 YOLOv8s（best.pt，nc=12）为主干，直接在 COCO 预训练权重上微调得到，覆盖手机、钱包、钥匙、书包、行李箱、笔记本电脑、校园卡、眼镜、笔记本、雨伞、水杯等 11 个常规类目，外加 1 个“其他”兜底类，无需运行时依赖开放词汇模型即可完成打标。同时保留 YOLO-Wor
[225] <Normal> 　　数据来源为多源公开数据集融合，统一映射到本系统定义的 11 个具体类别 + 1 个“其他”兜底类的体系（手机/钱包/钥匙/书包/行李箱/笔记本电脑/校园卡/眼镜/笔记本/雨伞/水杯 + 其他），“其他”类承接视觉无法明确归类的物品。具体包括：（1）COCO 2017：取其中与失物相关的 9 类（cell phone
[227] <Normal> 　　评价指标以目标检测通用指标为准：平均精度均值 mAP@0.5（IoU 阈值为 0.5 时的各类别 AP 均值），并以混淆矩阵（Confusion Matrix）刻画各类别被正确/错误分类的情况，识别常见混淆对（如“书包”与“手提包”）。验证过程：在自建测试集上运行推理，统计逐类 AP、整体 mAP@0.5，绘制混淆
[230] <Normal> 本系统的智能功能对应“图像目标检测”任务：输入用户上传的物品照片，输出物品类别标签（及置信度），用于后续结构化匹配。可选模型包括 Faster R-CNN（精度高但推理慢）、YOLO 系列（速度快、易部署）与开放词汇检测器 YOLO-World（零样本补专属类）。综合实时性与校园场景，本文以 YOLOv8s（COCO 
[234] <Normal> 　　数据来源为多源公开数据集融合，统一映射到本系统定义的 11 个具体类别 + 1 个“其他”兜底类的体系（手机/钱包/钥匙/书包/行李箱/笔记本电脑/校园卡/眼镜/笔记本/雨伞/水杯 + 其他），“其他”类承接视觉无法明确归类的物品。具体包括：（1）COCO 2017：取其中与失物相关的 9 类（cell phone
[236] <Normal> 表 4-1 多源训练数据集构成与类别映射（训练用）
[246] <Normal> 评价指标以目标检测通用指标为准：平均精度均值 mAP@0.5（IoU 阈值为 0.5 时的各类别 AP 均值），并以混淆矩阵（Confusion Matrix）刻画各类别被正确/错分的情况。mAP@0.5 反映整体定位与识别精度，混淆矩阵用于定位易混淆类对（如“笔记本”与“书”），指导提示词或阈值优化。
[250] <Normal> 注：表中数据为测试集（3,236 张图像）评测结果，由 tools/dataset_prep/eval_vision.py 一次性导出；第 12 类“其他类（other）”为兜底类别，不纳入核心检测精度考核；混淆矩阵标题维持“测试集 11 类”，即仅对 11 个常规类目绘制归一化混淆矩阵，其他类仅作兜底归类，不计入核心
[253] <Normal> 　　由表 4-2 可知，微调后的 YOLOv8s 在测试集（3,236 张图像）上整体 mAP@0.5 达到 0.654、mAP@0.5:0.95 为 0.483，精确率 0.704、召回率 0.605。分组来看，校园专属类（0.848）明显高于 COCO 通用类（0.683）——校园卡（AP@0.5 0.754）与眼
[254] <Normal> 　　在最终选型前，本文对边界框回归损失做了对照消融。两次训练除损失函数外的全部超参完全一致（同为 YOLOv8s 骨干、batch=8、epochs=120、lr0=0.01），仅将 ultralytics 原生 CIoU 替换为 WIoU v3（γ=1.9，通过替换 BboxLoss 注入），因此可将精度差异归因于损
[262] <Normal> 发布模块（PublishService）：拾得者上传照片与保管状态后，服务在进程内调用 VisionService.predict() 完成视觉打标，写入 found_item 并触发反向主动匹配——查询同类别、待认领的失物，交由 MatchService 打分，生成疑似匹配记录并推送。匹配模块（MatchServic
[266] <Normal> 　　当前已实现并验证的是后端最小闭环（发布→打标→匹配→认领→交接）：使用 SQLite 与视觉桩（MVP 阶段确定性返回类别标签）跑通端到端流程，自带开发库 dev.db（10 张表 + 12 分类种子数据）。在此基础上，单元测试覆盖加权打分纯函数（时间衰减、地点层级、关键词 Jaccard、七维加权与归一化、阈值判
[282] <Normal> 系统以后端最小闭环（发布→打标→匹配→认领→交接）跑通验证：使用 SQLite 与视觉桩（MVP 阶段确定性返回类别标签）完成端到端流程，自带开发库含 10 张表与 12 分类种子数据；前端以 Vue3 实现响应式页面并与后端联调。运行效果见图 4.3。
[291] <Normal> 　　测试环境：Windows 平台，Python 3.12；后端 FastAPI + uvicorn，SQLite（开发兜底库，零外部依赖即可运行）；测试框架 pytest，通过 FastAPI TestClient 真实发起 HTTP 请求；视觉为进程内桩（确定性返回类别，不加载 torch、不发网络），确保测试不依
[296] <Normal> 测试环境：Windows 平台，Python 3.12；后端 FastAPI + uvicorn，SQLite（开发兜底库，零外部依赖即可运行）；测试框架 pytest，通过 FastAPI TestClient 真实发起 HTTP 请求；视觉为进程内桩（确定性返回类别，不加载 torch、不发网络），确保测试不依赖模
[316] <Normal> 　　本文围绕“校园失物招领”这一高频校园需求，完成了一套基于 YOLOv8 的智能匹配系统的需求分析、设计、实现与测试。主要成果如下：（1）提出并实现了“视觉自动打标 + 七维加权匹配 + 防冒领闭环”的端到端方案，拾得者发布仅需拍照与保管状态，系统自动归类并主动推送疑似匹配；（2）将 YOLOv8s（COCO 常用类
[317] <Normal> 　　工作过程中也遇到并解决了若干关键问题：测试套件初期的共享状态导致偶发不稳定，通过函数级隔离 fixture 修复；独立视觉服务带来的额外开销与超时风险，经论证改为进程内推理；训练阶段尝试以 WIoU 损失替换 CIoU，因本数据集类别高度不均衡导致稀有类召回崩溃（验证集 mAP@0.5 由 0.710 跌至 0.0

## 二、类目枚举上下文（命中'类目/类别/12类/11类'段落 + 前后1段）

   [20] <Normal> 【摘 要】
>>>[21] <Normal> 　　随着高校办学规模扩大与校园生活节奏加快，校园内遗失与拾得物品的事件频发。传统失物招领依赖布告栏、微信群、QQ 群等渠道，存在信息传播范围有限、检索效率低、易冒领等问题。针对上述问题，本文设计并实现了一个基于 YOLOv8 的校园失物招领智能匹配系统。系统采用前后端分离架构：后端基于 Python FastAPI 与
   [22] <Normal> 【关键词】
   [79] <Normal> 　　校园失物招领是高校后勤与学生日常管理中高频且长期存在的需求。随着在校生规模扩大、跨校区活动增多，书包、手机、校园卡、钥匙、雨伞等随身物品遗失与拾得事件几乎每天发生。然而，当前多数高校的失物招领仍依赖线下布告栏、食堂公告、以及微信/QQ 群等非结构化渠道：失主需要逐群刷消息、人工比对描述，拾得者缺乏统一发布入口，信息
>>>[80] <Normal> 　　针对以上痛点，本课题研发一套“基于 YOLOv8 的校园失物招领智能匹配系统”。其核心思路是：用计算机视觉自动识别物品类别，把“人找物”的模糊检索转化为“类别+时空+语义”的结构化匹配；用一套防冒领闭环（审计+动态交接码+申诉）保证交接可信。选题兼具工程价值与研究意义：一方面可直接服务校园师生、降低失物找回成本；另
   [81] <Normal> 　　本章作为全文导引，首先说明课题背景、目的与意义，随后综述国内外相关系统的研发现状，明确本文的主要工作，最后给出论文的章节组织结构。
   [82] <Normal> 　　从实现形态看，失物招领相关系统经历了三个阶段。（1）布告栏/表格阶段：以 BBS、校园论坛置顶帖、在线协作文档（如共享表格）为代表，信息集中但检索靠人工，匹配效率低。（2）专用小程序/App 阶段：近年出现了多款“失物招领”类微信小程序与校园应用，提供发布、列表、搜索功能，部分引入了 LBS 地理位置与消息通知，例
>>>[83] <Normal> 　　在核心技术侧，目标检测算法从两阶段（Faster R-CNN）演进到单阶段（YOLO 系列、SSD），再到无锚框（anchor-free）的 YOLOv8，在精度与速度上取得良好平衡，已广泛用于商品识别、工业质检等场景。2023 年提出的 YOLO-World 进一步支持“开放词汇/零样本”检测，可用文本提示直接识
   [84] <Normal> 　　综上，现有系统普遍缺少“视觉自动归类 + 结构化智能匹配 + 防冒领溯源”的端到端闭环。本文工作即定位于此：以微调 YOLOv8s（COCO 预训练权重微调，覆盖 11 类校园高频失物）+ YOLO-World（零样本补校园专属类）为视觉底座，构建一套发布即打标、自动推送疑似匹配、双端确认交接的可信失物招领系统。
   [84] <Normal> 　　综上，现有系统普遍缺少“视觉自动归类 + 结构化智能匹配 + 防冒领溯源”的端到端闭环。本文工作即定位于此：以微调 YOLOv8s（COCO 预训练权重微调，覆盖 11 类校园高频失物）+ YOLO-World（零样本补校园专属类）为视觉底座，构建一套发布即打标、自动推送疑似匹配、双端确认交接的可信失物招领系统。
>>>[85] <Normal> 　　本文主要工作包括：（1）需求与架构设计：面向失主、拾得者、管理员、游客四类角色，梳理功能性与非功能性需求，给出分层架构、数据库 Schema（10 张表）与 RESTful API 规范；（2）进程内视觉打标：将 YOLOv8s 与 YOLO-World 推理以进程内方式嵌入后端，拾得者发布物品时自动完成类别识别，
   [86] <Normal> 　　本论文主体由七章组成。第一章绪论介绍研发背景、国内外现状与本文主要工作；第二章阐述系统所依赖的核心技术（YOLOv8/YOLO-World 与 FastAPI 等）；第三章进行系统需求分析与设计（数据库、接口、架构、模块）；第四章说明机器学习模型的选型、数据集、训练与验证方案；第五章描述系统实现环境与核心模块实现；
   [87] <Heading 2> 引言
>>>[88] <Normal> 本课题所属研发领域为智慧校园与计算机视觉交叉方向下的失物招领信息系统。随着高校在校生规模扩大与跨校区活动增多，书包、手机、校园卡、钥匙、雨伞等随身物品遗失与拾得几乎每日发生；而传统失物招领长期依赖线下布告栏、食堂公告与微信/QQ 群等非结构化渠道，存在信息传播范围有限、时效性差、匹配效率低等突出问题，且“凭文字描述难核
   [89] <Normal> 
   [91] <Heading 2> 国内外研发现状
>>>[92] <Normal> 　　当前国内外在失物招领系统与物品识别方向的研究可归纳为三类。（1）校园失物招领类微信小程序与自建平台。以“拾到空间”等校园失物招领小程序[5]为代表，提供发布、列表、搜索与消息通知功能，多以关键词/分类浏览为主，物品描述依赖用户手工录入，图像理解能力薄弱，匹配高度依赖用户主动比对，效率有限，构成本系统的需求对照基线。
   [93] <Normal> 
   [94] <Heading 2> 论文主要工作
>>>[95] <Normal> 本文研发设想是构建一套 Web 端“基于 YOLOv8 的校园失物招领智能匹配系统”：以计算机视觉自动识别物品类别，把模糊检索转化为结构化匹配，并以可信交接闭环保证归属可溯源。研发方法采用前后端分离架构：后端基于 FastAPI+SQLAlchemy 2.x[1][7]，将微调 YOLOv8s 与 YOLO-World
   [96] <Normal> 
   [101] <Normal> 　　本章简要介绍与系统研发密切相关的核心技术背景，后续章节内容均建立在这些技术之上。主要包括目标检测领域的 YOLOv8 与 YOLO-World，以及支撑后端与前端的技术栈 FastAPI、SQLAlchemy、MySQL、Redis、JWT、WebSocket 与 Vue3。
>>>[102] <Normal> YOLO（You Only Look Once）是将目标检测建模为单次回归问题的单阶段检测器，以速度快、易部署著称。YOLOv8 由 Ultralytics 于 2023 年发布，采用无锚框（anchor-free）解耦头与 CIoU 损失，在 COCO 数据集上取得精度与推理速度的较好平衡，并提供 n/s/m/l/x
   [103] <Normal> 　　YOLO-World 是 2023 年提出的“开放词汇（open-vocabulary）目标检测”模型，基于视觉-语言对齐（如 CLIP 文本编码器）实现零样本检测：用户只需以文本提示（prompt）描述待检类别（如“campus card, student ID”“keychain with keys”“othe
   [102] <Normal> YOLO（You Only Look Once）是将目标检测建模为单次回归问题的单阶段检测器，以速度快、易部署著称。YOLOv8 由 Ultralytics 于 2023 年发布，采用无锚框（anchor-free）解耦头与 CIoU 损失，在 COCO 数据集上取得精度与推理速度的较好平衡，并提供 n/s/m/l/x
>>>[103] <Normal> 　　YOLO-World 是 2023 年提出的“开放词汇（open-vocabulary）目标检测”模型，基于视觉-语言对齐（如 CLIP 文本编码器）实现零样本检测：用户只需以文本提示（prompt）描述待检类别（如“campus card, student ID”“keychain with keys”“othe
   [104] <Normal> FastAPI 是基于 Python asyncio 的现代 Web 框架，以类型注解驱动自动生成 OpenAPI 文档、依赖注入（Depends）管理服务与鉴权、原生支持异步，非常适合构建 JSON 风格、平台无关的后端接口（Web 与小程序共用）。SQLAlchemy 2.x 是 Python 主流 ORM，通过声
   [104] <Normal> FastAPI 是基于 Python asyncio 的现代 Web 框架，以类型注解驱动自动生成 OpenAPI 文档、依赖注入（Depends）管理服务与鉴权、原生支持异步，非常适合构建 JSON 风格、平台无关的后端接口（Web 与小程序共用）。SQLAlchemy 2.x 是 Python 主流 ORM，通过声
>>>[105] <Normal> 　　上述技术中，YOLOv8 与 YOLO-World 构成系统的“视觉大脑”，负责把用户上传的照片转化为结构化类别标签；FastAPI+SQLAlchemy+MySQL+Redis 构成“业务骨架”，负责任务编排、持久化与高并发；Vue3+Element Plus 构成“交互界面”；JWT 与 WebSocket 解
   [106] <Heading 2> 核心技术1
   [110] <Heading 2> 核心技术2
>>>[111] <Normal> YOLO-World 是 2023 年提出的开放词汇（Open-Vocabulary）目标检测模型，基于 CLIP 文本编码器实现零样本检测：用户只需以文本提示词描述类别，模型即可识别训练时未出现的新物体，突破了闭集检测器的类别天花板。[10][4]
   [112] <Normal> 尽管微调后的 YOLOv8s 已覆盖多数常用类，但“校园卡、钥匙、笔记本”等校园专属物品并不在 COCO 之中。为此，本系统保留 YOLO-World 作为可插拔的零样本兜底：以“campus card / key / notebook”等提示词识别上述专属类，与 YOLOv8s 形成“通用+专属”的双路视觉底座，且同
   [117] <Normal> 　　本章对系统进行需求分析、数据库与接口设计、软件架构设计与详细模块设计，体现“分析—设计”的思路与过程。需求依据任务书与 EARS 格式需求规格说明书，系统面向四类角色：失主（丢失物品、发布寻物、认领）、拾得者（拾得物品、发布招领、确认归还）、管理员（审计、封禁、分类管理）、游客（浏览已解决公示栏）。
>>>[118] <Normal> 　　系统功能性需求（节选）包括：FR-1 用户注册/登录（双因子短信验证码）；FR-2 失主发布失物（含类别、地点、时间、描述）；FR-3 拾得者发布拾物（拍照+保管状态二选一，零门槛）；FR-4 系统自动视觉打标；FR-5 反向主动匹配并推送疑似结果；FR-6 失主认领（填写认领理由作为凭证）；FR-7 动态交接码双
   [119] <Normal> 　　系统核心数据对象包括用户、物品分类、失物、拾物、匹配记录、交接码、即时通讯会话/消息、信誉流水、审计日志。其实体-联系（E-R）模型如图 3.1 所示。一个用户可发布多条失物/拾物；一条分类可标注多条失物/拾物；一条失物与一条拾物可构成一条匹配记录；一条匹配可生成多序号交接码、可开启一个 IM 会话；用户产生信誉流
   [121] <Normal> 图 3.1 系统 E-R 图
>>>[122] <Normal> 　　数据字典（DD）对核心字段给出类型与约束，例如用户表含学号（唯一登录账号）、手机号（双因子）、信誉分（默认 100）；失物/拾物表含类别外键、6 位行政区划码 region_code、状态机字段；审计表含操作类型、目标对象、IP/UA/会话/GPS 与原文明细，并按月分区。
   [123] <Normal> 　　系统功能通过 UML 用例图建模，如图 3.4 所示：失主使用“发布失物/查看匹配/认领/验证交接码/IM 沟通”；拾得者使用“发布拾物/确认归还/生成交接码/IM 沟通”；管理员使用“审计时间线/封禁/分类管理”；游客使用“浏览公示栏”。核心业务“发布→打标→匹配→认领→交接”的 UML 序列图如图 3.5 所示
   [142] <Normal> 　　匹配引擎是本系统的算法核心。给定一条失物 l 与一条拾物 f，匹配度 score 由「分类 + 文字语义 + 时间」七子维度加权得到（v2 评分引擎，详见式 3-2）。
>>>[143] <Normal> score = 20·(photo⊕category) + 15·qty + 20·color + 10·state + 15·place + 10·keyword + 10·time      (式 3-2，各子项∈[0,1]，权重和=100)
　　归一化：设 W 为失主实际提供维度的权重之和，k = 100 / m
   [144] <Normal> 　　评分引擎经历了两轮演进：初版采用“图像相似、类别、外观、特征、时间、地点”六维加权（式 3-1 旧），但外观/特征与文字属性、地点与文字地点存在语义重合，且权重固定未归一化，导致“失主描述越完整得分反而未必越高”。第二轮将冗余维度并入统一的文字语义维度，形成“分类 + 文字 + 时间”的精简结构；最终 v2 版进一
   [143] <Normal> score = 20·(photo⊕category) + 15·qty + 20·color + 10·state + 15·place + 10·keyword + 10·time      (式 3-2，各子项∈[0,1]，权重和=100)
　　归一化：设 W 为失主实际提供维度的权重之和，k = 100 / m
>>>[144] <Normal> 　　评分引擎经历了两轮演进：初版采用“图像相似、类别、外观、特征、时间、地点”六维加权（式 3-1 旧），但外观/特征与文字属性、地点与文字地点存在语义重合，且权重固定未归一化，导致“失主描述越完整得分反而未必越高”。第二轮将冗余维度并入统一的文字语义维度，形成“分类 + 文字 + 时间”的精简结构；最终 v2 版进一
   [145] <Normal> 　　本章完成了系统从需求到设计的完整推导：明确了四类角色与九项功能性需求；设计了 10 张表的关系模型与索引策略；给出了分层架构、进程内视觉方案与统一接口规范；并以类图、序列图、状态图与加权打分公式刻画了核心模块。下一章将说明支撑视觉识别的机器学习模型训练与验证方案。
   [210] <Normal> 对系统的详细设计进行概述，描述系统详细设计的基本方法、过程、工具等，可以参考教科书的内容。
>>>[211] <Normal> 匹配模块（MatchService）是算法核心。给定一条失物 l 与一条拾物 f，匹配度 score 由「分类 + 文字语义 + 时间」七子维度加权得到（v2 评分引擎）：score = 20·(photo⊕category) + 15·qty + 20·color + 10·state + 15·place + 10
   [212] <Normal> 候选集先经类别主键过滤，再按归一化后的 total 降序返回；低于阈值（threshold=80，疑似线）的匹配不予推送，但“前 10 条保底 + 所有≥80 疑似全列”的护栏避免漏召回。photo_category、qty、color、state、place、keyword、time 七维打分与归一化系数 norm_
   [211] <Normal> 匹配模块（MatchService）是算法核心。给定一条失物 l 与一条拾物 f，匹配度 score 由「分类 + 文字语义 + 时间」七子维度加权得到（v2 评分引擎）：score = 20·(photo⊕category) + 15·qty + 20·color + 10·state + 15·place + 10
>>>[212] <Normal> 候选集先经类别主键过滤，再按归一化后的 total 降序返回；低于阈值（threshold=80，疑似线）的匹配不予推送，但“前 10 条保底 + 所有≥80 疑似全列”的护栏避免漏召回。photo_category、qty、color、state、place、keyword、time 七维打分与归一化系数 norm_
   [213] <Normal> 　　匹配池交互遵循“单向可见”原则：拾得者选择“留在原地”保管（keep1）的物品仅单向进入失主匹配池——失主侧可见并可主动认领，拾得者侧仅只读呈现、不反向生成候选，避免对“已放回原地、无需再介入”的拾得者造成重复打扰；低分候选（total<60）以弱化卡片 + 二次确认方式呈现，兼顾召回与防误领；疑似阈值维持 80，
   [215] <Normal> 
>>>[216] <Normal> 发布模块（PublishService）负责“零门槛发布”：拾得者上传照片与保管状态后，服务在进程内调用 VisionService.predict() 完成视觉打标并写入 found_item，随即触发反向主动匹配——查询同类别、待认领的失物交由 MatchService 打分，生成疑似匹配记录并推送。
   [217] <Normal> 交接与审计模块（HandoverService / AuditService）构成防冒领闭环：认领确认后生成 6 位动态交接码，Redis 作活性存储（TTL=30 分钟，兼作并发唯一与限流），MySQL handover_code 表作不可变审计镜像；双端（失主/拾得者）扫码验证均通过后状态置为“已解决”。审计模块对
   [223] <Normal> 　　本章说明系统中视觉识别所用机器学习模型的构建方法、过程与验证方案，包括模型选择与任务定义、数据集构建与预处理、模型训练与模型验证。
>>>[224] <Normal> 　　本系统的视觉底座以单路微调 YOLOv8s（best.pt，nc=12）为主干，直接在 COCO 预训练权重上微调得到，覆盖手机、钱包、钥匙、书包、行李箱、笔记本电脑、校园卡、眼镜、笔记本、雨伞、水杯等 11 个常规类目，外加 1 个“其他”兜底类，无需运行时依赖开放词汇模型即可完成打标。同时保留 YOLO-Wor
   [225] <Normal> 　　数据来源为多源公开数据集融合，统一映射到本系统定义的 11 个具体类别 + 1 个“其他”兜底类的体系（手机/钱包/钥匙/书包/行李箱/笔记本电脑/校园卡/眼镜/笔记本/雨伞/水杯 + 其他），“其他”类承接视觉无法明确归类的物品。具体包括：（1）COCO 2017：取其中与失物相关的 9 类（cell phone
   [224] <Normal> 　　本系统的视觉底座以单路微调 YOLOv8s（best.pt，nc=12）为主干，直接在 COCO 预训练权重上微调得到，覆盖手机、钱包、钥匙、书包、行李箱、笔记本电脑、校园卡、眼镜、笔记本、雨伞、水杯等 11 个常规类目，外加 1 个“其他”兜底类，无需运行时依赖开放词汇模型即可完成打标。同时保留 YOLO-Wor
>>>[225] <Normal> 　　数据来源为多源公开数据集融合，统一映射到本系统定义的 11 个具体类别 + 1 个“其他”兜底类的体系（手机/钱包/钥匙/书包/行李箱/笔记本电脑/校园卡/眼镜/笔记本/雨伞/水杯 + 其他），“其他”类承接视觉无法明确归类的物品。具体包括：（1）COCO 2017：取其中与失物相关的 9 类（cell phone
   [226] <Normal> 本系统在 COCO 预训练权重基础上开展微调（fine-tuning），而非仅用固定预训练权重，以提升校园专属类的识别精度。训练在配备 NVIDIA GPU 的机器上进行，框架为 Ultralytics YOLOv8（Python 3.12 + PyTorch CUDA），主干选用 YOLOv8s；输入尺寸 img_s
   [226] <Normal> 本系统在 COCO 预训练权重基础上开展微调（fine-tuning），而非仅用固定预训练权重，以提升校园专属类的识别精度。训练在配备 NVIDIA GPU 的机器上进行，框架为 Ultralytics YOLOv8（Python 3.12 + PyTorch CUDA），主干选用 YOLOv8s；输入尺寸 img_s
>>>[227] <Normal> 　　评价指标以目标检测通用指标为准：平均精度均值 mAP@0.5（IoU 阈值为 0.5 时的各类别 AP 均值），并以混淆矩阵（Confusion Matrix）刻画各类别被正确/错误分类的情况，识别常见混淆对（如“书包”与“手提包”）。验证过程：在自建测试集上运行推理，统计逐类 AP、整体 mAP@0.5，绘制混淆
   [228] <Normal> 　　本章明确了以微调 YOLOv8s + YOLO-World 双路检测作为视觉底座，给出了数据集构建、训练参数与以 mAP@0.5 + 混淆矩阵为核心的验证方案，为系统“发布即打标”提供模型支撑。
   [229] <Heading 2> 4.1模型选择与任务定义
>>>[230] <Normal> 本系统的智能功能对应“图像目标检测”任务：输入用户上传的物品照片，输出物品类别标签（及置信度），用于后续结构化匹配。可选模型包括 Faster R-CNN（精度高但推理慢）、YOLO 系列（速度快、易部署）与开放词汇检测器 YOLO-World（零样本补专属类）。综合实时性与校园场景，本文以 YOLOv8s（COCO 
   [231] <Normal> 描述可选模型，并论证选择相关模型的依据，包括模型的基本特点、适用场景、完成本任务的优势等，明确模型在系统开发中的作用。
   [233] <Heading 2> 4.2数据集构建与预处理
>>>[234] <Normal> 　　数据来源为多源公开数据集融合，统一映射到本系统定义的 11 个具体类别 + 1 个“其他”兜底类的体系（手机/钱包/钥匙/书包/行李箱/笔记本电脑/校园卡/眼镜/笔记本/雨伞/水杯 + 其他），“其他”类承接视觉无法明确归类的物品。具体包括：（1）COCO 2017：取其中与失物相关的 9 类（cell phone
   [235] <Normal> 预处理包含：尺寸归一化至模型输入（YOLOv8s img_size=640）；格式统一为 JPEG/RGB；对少样本专属类做轻度数据增强（翻转、亮度抖动）以缓解不均衡；缺失 EXIF 的样本以用户手动选点补齐 region_code。所有图像经去噪与中心裁剪后送入推理。
   [235] <Normal> 预处理包含：尺寸归一化至模型输入（YOLOv8s img_size=640）；格式统一为 JPEG/RGB；对少样本专属类做轻度数据增强（翻转、亮度抖动）以缓解不均衡；缺失 EXIF 的样本以用户手动选点补齐 region_code。所有图像经去噪与中心裁剪后送入推理。
>>>[236] <Normal> 表 4-1 多源训练数据集构成与类别映射（训练用）
   [237] <Normal> 
   [245] <Normal> （1）模型验证指标
>>>[246] <Normal> 评价指标以目标检测通用指标为准：平均精度均值 mAP@0.5（IoU 阈值为 0.5 时的各类别 AP 均值），并以混淆矩阵（Confusion Matrix）刻画各类别被正确/错分的情况。mAP@0.5 反映整体定位与识别精度，混淆矩阵用于定位易混淆类对（如“笔记本”与“书”），指导提示词或阈值优化。
   [247] <Normal> （2）模型验证和结果分析
   [249] <Normal> 表 4-2 模型验证结果（mAP@0.5 与混淆矩阵）
>>>[250] <Normal> 注：表中数据为测试集（3,236 张图像）评测结果，由 tools/dataset_prep/eval_vision.py 一次性导出；第 12 类“其他类（other）”为兜底类别，不纳入核心检测精度考核；混淆矩阵标题维持“测试集 11 类”，即仅对 11 个常规类目绘制归一化混淆矩阵，其他类仅作兜底归类，不计入核心
   [251] <Normal> 
   [253] <Normal> 　　由表 4-2 可知，微调后的 YOLOv8s 在测试集（3,236 张图像）上整体 mAP@0.5 达到 0.654、mAP@0.5:0.95 为 0.483，精确率 0.704、召回率 0.605。分组来看，校园专属类（0.848）明显高于 COCO 通用类（0.683）——校园卡（AP@0.5 0.754）与眼
>>>[254] <Normal> 　　在最终选型前，本文对边界框回归损失做了对照消融。两次训练除损失函数外的全部超参完全一致（同为 YOLOv8s 骨干、batch=8、epochs=120、lr0=0.01），仅将 ultralytics 原生 CIoU 替换为 WIoU v3（γ=1.9，通过替换 BboxLoss 注入），因此可将精度差异归因于损
   [255] <Normal> 
   [261] <Normal> 开发环境：后端 Python 3.12，框架 FastAPI + SQLAlchemy 2.x，ASGI 服务器 uvicorn；关系数据库 MySQL 8.0（开发期以 SQLite 兜底，保证无外部依赖即可跑通），缓存/活性存储 Redis（以 Docker 一键启动，避免写入系统盘）；前端 Vue3 + Vite
>>>[262] <Normal> 发布模块（PublishService）：拾得者上传照片与保管状态后，服务在进程内调用 VisionService.predict() 完成视觉打标，写入 found_item 并触发反向主动匹配——查询同类别、待认领的失物，交由 MatchService 打分，生成疑似匹配记录并推送。匹配模块（MatchServic
   [263] <Normal> 
   [265] <Normal> # 加权打分（v2 节选伪代码）
def score(lost, found):
    if lost.category_id != found.category_id: return None   # 不同类不进打分
    fl = extract_features(lost, is_lost=True)    
>>>[266] <Normal> 　　当前已实现并验证的是后端最小闭环（发布→打标→匹配→认领→交接）：使用 SQLite 与视觉桩（MVP 阶段确定性返回类别标签）跑通端到端流程，自带开发库 dev.db（10 张表 + 12 分类种子数据）。在此基础上，单元测试覆盖加权打分纯函数（时间衰减、地点层级、关键词 Jaccard、七维加权与归一化、阈值判
   [267] <Normal> 　　本章在既定技术栈下完成了后端核心模块的编码：进程内视觉打标、七维加权匹配、动态交接码双写与审计留痕。测试表明最小闭环可稳定运行，为下一章系统测试所验证。
   [281] <Heading 2> 5.3系统运行
>>>[282] <Normal> 系统以后端最小闭环（发布→打标→匹配→认领→交接）跑通验证：使用 SQLite 与视觉桩（MVP 阶段确定性返回类别标签）完成端到端流程，自带开发库含 10 张表与 12 分类种子数据；前端以 Vue3 实现响应式页面并与后端联调。运行效果见图 4.3。
   [283] <List Paragraph> 系统运行的效果图如图4.3所示：
   [290] <Normal> 　　系统测试旨在验证功能正确性与稳定性，采用单元测试与端到端（集成）测试相结合的方法，参考“测试计划/测试报告”技术文档规范。
>>>[291] <Normal> 　　测试环境：Windows 平台，Python 3.12；后端 FastAPI + uvicorn，SQLite（开发兜底库，零外部依赖即可运行）；测试框架 pytest，通过 FastAPI TestClient 真实发起 HTTP 请求；视觉为进程内桩（确定性返回类别，不加载 torch、不发网络），确保测试不依
   [292] <Normal> 　　测试设计遵循“先单元、后集成”：对无状态纯函数（时间衰减、地点层级命中、关键词 Jaccard、加权求和与阈值判定）编写独立单元测试，验证公式与边界；对闭环编写端到端测试，用 TestClient 真跑“注册→登录→发布失物(带图)→查匹配(按 score 降序)→发同类拾物→认领(填理由)→确认归还→生成交接码→
   [295] <Normal> 　　本章通过单元与端到端测试对系统进行了验证：334 例用例全部通过，核心闭环与错误路径行为符合设计，加权匹配算法经单测确认正确。系统在功能完整性与稳定性上达到了毕设预期。
>>>[296] <Normal> 测试环境：Windows 平台，Python 3.12；后端 FastAPI + uvicorn，SQLite（开发兜底库，零外部依赖即可运行）；测试框架 pytest，通过 FastAPI TestClient 真实发起 HTTP 请求；视觉为进程内桩（确定性返回类别，不加载 torch、不发网络），确保测试不依赖模
   [297] <Normal> 描述系统测试所需要的硬件条件、软件环境等，以及拟采用的系统测试平台、工具等。
   [315] <Heading 1> 总结及展望
>>>[316] <Normal> 　　本文围绕“校园失物招领”这一高频校园需求，完成了一套基于 YOLOv8 的智能匹配系统的需求分析、设计、实现与测试。主要成果如下：（1）提出并实现了“视觉自动打标 + 七维加权匹配 + 防冒领闭环”的端到端方案，拾得者发布仅需拍照与保管状态，系统自动归类并主动推送疑似匹配；（2）将 YOLOv8s（COCO 常用类
   [317] <Normal> 　　工作过程中也遇到并解决了若干关键问题：测试套件初期的共享状态导致偶发不稳定，通过函数级隔离 fixture 修复；独立视觉服务带来的额外开销与超时风险，经论证改为进程内推理；训练阶段尝试以 WIoU 损失替换 CIoU，因本数据集类别高度不均衡导致稀有类召回崩溃（验证集 mAP@0.5 由 0.710 跌至 0.0
   [316] <Normal> 　　本文围绕“校园失物招领”这一高频校园需求，完成了一套基于 YOLOv8 的智能匹配系统的需求分析、设计、实现与测试。主要成果如下：（1）提出并实现了“视觉自动打标 + 七维加权匹配 + 防冒领闭环”的端到端方案，拾得者发布仅需拍照与保管状态，系统自动归类并主动推送疑似匹配；（2）将 YOLOv8s（COCO 常用类
>>>[317] <Normal> 　　工作过程中也遇到并解决了若干关键问题：测试套件初期的共享状态导致偶发不稳定，通过函数级隔离 fixture 修复；独立视觉服务带来的额外开销与超时风险，经论证改为进程内推理；训练阶段尝试以 WIoU 损失替换 CIoU，因本数据集类别高度不均衡导致稀有类召回崩溃（验证集 mAP@0.5 由 0.710 跌至 0.0
   [318] <Normal> 　　后续工作可从两方面展开：算法侧，用自建校园测试集完成模型验证与轻量微调，并以实验结果反哺权重/阈值；系统侧，进一步完善 IM 实时消息与公示/管理模块的交互体验。总体而言，本文为校园失物招领提供了一套自动化、可追溯、可扩展的参考实现。

## 三、表格内容（前3张表，找类目枚举）


### 表 0

  r0: ['表名', '关键字段', '说明']
  r1: ['user', 'id, student_no(UK), phone(UK), password_hash, role(0普通/1管理员), credit_score(默认100), status', '用户']
  r2: ['category', 'id, name, yolo_class_id, recognition_mode(0=COCO/1=YOLO-World), yolo_prompt, parent_id, is_active', '11 常规 + 1 其他(id=None)']
  r3: ['lost_item', 'publisher_id(FK), category_id(FK), category_name, title, description, images(JSON), color, tags(JSON), image_hash, appearance, features, location, lost_time, status, expires_at, deleted_at', 'v8 三列：appearance/features/location']
  r4: ['found_item', 'finder_id(FK), category_id(FK), category_name, description, images(JSON), tags, image_hash, appearance, features, location, found_time, keep_status, contact_allowed, status, expires_at, deleted_at', '同上']
  r5: ['match_record', 'lost_id(FK), found_id(FK), match_score, status, claim_reason, code, code_expire, completed_at', '匹配记录']
  r6: ['handover_code', 'match_id(FK), seq, code, qr_token, status, verified_by_lost, verified_by_finder, gps_lost, gps_finder, generated_at, expire_at', '交接码审计镜像']
  r7: ['im_session', 'match_id(FK,可空), found_id(FK,可空), lost_user_id(FK), finder_user_id(FK), status, last_message_at, expires_at', 'IM 会话（轮询，非 WebSocket）']
  r8: ['im_message', 'session_id(FK), sender_id(FK), sender_role, content_type, content', 'IM 消息']
  r9: ['trust_score_log', 'user_id(FK), delta, reason, ref_type, ref_id', '信誉流水']
  r10: ['audit_log', 'user_id(FK), action, target_type, target_id, ip, ua, session_id, gps, detail, created_at(按月分区)', '审计黑匣子']

### 表 1

  r0: ['数据集', '原始类别', '映射至本系统类', '样本规模（约）', '用途']
  r1: ['COCO 2017', '80 类（取 9 类子集）', 'phone / schoolbag / suitcase / laptop / notebook / umbrella / bottle 等 8 类', 'train≈11.8 万图（取目标类子集）', '预训练 + 微调主体']
  r2: ['LeftInCar', '10 类（过滤 clothing）', 'phone / wallet / keys / schoolbag / suitcase / laptop / campus_card / glasses', '971 图', '补充校园高频小物']
  r3: ['HomeObjects-3K', '11 类家居', '仅 laptop', '2285 train / 404 val', 'laptop 类补充']
  r4: ['合并后（final）', '11 类 + 其他（nc=12）', '统一标签体系', '80,863 图（train 59,309 / val 18,318 / test 3,236）', '最终训练 / 验证 / 测试']

### 表 2

  r0: ['指标', 'COCO 通用类（9 类）', '校园专属类', '整体', '其他类']
  r1: ['mAP@0.5', '0.683', '0.848', '0.654', '0.008']
  r2: ['mAP@0.5:0.95', '0.500', '0.647', '0.483', '0.007']
  r3: ['混淆矩阵', '图4-4', '图4-4', '图4-4', '图4-4']