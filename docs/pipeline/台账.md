# 失物招领系统 · 交付流水线台账

- 模式：D 跳步优化 ｜ 落盘目录：`docs/pipeline/` ｜ 创建：2026-09-23
- 当前状态：进行中
- 本轮范围（用户 09-23 拍板）：只做代码侧收尾（LICENSE+CORS 收敛+杂物清理）；真实上线运营暂不启动

## 一、在做（最多一张）

| 卡 | 执行者 | 开工时间 | 备注 |
|---|---|---|---|
| （空，本轮 RAG 流水线优先，失物侧待用户拍板卡#2） | | | |

## 二、待执行（按依赖顺序）

| 卡 | 标题 | 依赖 | 可并行 | 验收要点 |
|---|---|---|---|---|
| （空） | | | | |

## 三、做完（勾选区）

- [x] 卡#1 代码侧收尾（09-23：LICENSE(MIT)+CORS 收敛至 localhost:5173 白名单+杂物清理（`3\` 因内含 3 份有价值文档改为归档 docs/attic/3_docs/，唯一偏差）；/health 200 且进程杀净。~~实测发现 pytest 全量 55 failed~~ **后经卡#2 双轮实测证伪，见下**）
- [x] 卡#2 测试顺序依赖修复（09-23：**"55 failed"未复现，系一次性环境假象**——当时两个并发派遣的进程互删共享测试库 tests/_mvp_qa.db 所致；正序+42 文件倒序双轮全量均 394 passed/0 failed EXIT=0；真 bug 无、过期断言无；零代码改动（外科纪律：无可修之败）；CHANGELOG 已补如实澄清条目；遗留卫生建议：tests/test_zz_diag.py 临时诊断文件待维护者裁决）
- [x] 卡#3 安全配置与凭据治理（09-23：JWT_SECRET 弱默认废除+启动 fail fast（空值/dev- 前缀/占位符三证拒绝）；ADMIN_APPLY_CODE 出库（本机 .env 保留 110，用户好记需求）；DEBUG 连坐拆除→独立开关 SHOW_SMS_CODE+RATE_LIMIT_ENABLED；compose 3306 改本机回环+口令全部环境变量化；scripts/seed.py 演示账号 SEED_DEMO 门控（核实指控属实，启动路径本就不播演示账号）；新增请求体 413 护栏；bcrypt/注入抽查/CORS 复核通过；报告 `docs/pipeline/安检报告.md`；pytest 全量 394 passed/2 skipped/0 failed EXIT=0（串行）；遗留 3 条记报告：/users/me 全量自查缺失、LoginView dev_code 兜底 123456、--admin-pwd 建议必填）
- [x] 收尾：git 收尾一提 82c973c + push origin master ✓；7726716（卡#3+小抄移出）已推送 ✓

- [ ] 卡#4 交接码加固（role 服务端推导+错5次锁定+恒时比较+先查过期）——下一张
- [x] 卡#5 IM 串线修复（09-23：found_id 复用查询加参与者过滤 `OR(lost_user_id, finder_user_id)`（ORM 参数绑定），他人活跃会话不复用、为当前用户新建独立会话（同一拾物多条一对一私聊线）；核实 im_session.found_id 无唯一约束免迁移；match_id 路径有前置参与者校验未动；新增双用户独立会话用例（各自发消息 200、互不可见 403、A 复用回归）；既有 IM 18 用例回归；pytest 全量 397 passed/2 skipped/0 failed EXIT=0（串行）；py_compile 通过；git status 仅限清单）
- [ ] 卡#6 考古清理（五套 deprecated 权重+300 行旧公式+.env.example 对齐）
- [ ] 卡#7 async 架构（YOLO 推理移出事件循环）
- [ ] 安检 L2 档（真机部署前必跑，当前未跑）；08ddd8e 已推送 ✓

## 四、阻塞区

| 卡 | 阻塞原因 | 需要谁 |
|---|---|---|
| （空） | | |

## 备注
- 清理前若 `git status` 有未提交改动，子代理须停下报告，不擅自混入。
