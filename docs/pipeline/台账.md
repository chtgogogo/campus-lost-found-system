# 失物招领系统 · 交付流水线台账

- 模式：D 跳步优化 ｜ 落盘目录：`docs/pipeline/` ｜ 创建：2026-09-23
- 当前状态：进行中
- 本轮范围（用户 09-23 拍板）：只做代码侧收尾（LICENSE+CORS 收敛+杂物清理）；真实上线运营暂不启动

## 一、在做（最多一张）

| 卡 | 执行者 | 开工时间 | 备注 |
|---|---|---|---|
| （空，本轮 RAG 流水线优先，失物侧待用户拍板卡#2） | | | |

## 二、待执行（按依赖顺序）

| 卡 | 标题 | 依赖 | 可并行 | 验收要点 |
|---|---|---|---|---|
| 卡#2 | 测试顺序依赖修复（**待用户拍板是否立项**） | 卡#1 | — | pytest 全量 0 failed；CHANGELOG 声称与实测一致 |

## 三、做完（勾选区）

- [x] 卡#1 代码侧收尾（09-23：LICENSE(MIT)+CORS 收敛至 localhost:5173 白名单+杂物清理（`3\` 因内含 3 份有价值文档改为归档 docs/attic/3_docs/，唯一偏差）；/health 200 且进程杀净。**实测发现**：pytest 全量 55 failed/339 passed，抽查单跑全过=既有测试顺序依赖问题，与本卡无关；CHANGELOG"全量通过"声称与实测不符，待卡#2 处理）

## 四、阻塞区

| 卡 | 阻塞原因 | 需要谁 |
|---|---|---|
| （空） | | |

## 备注
- 清理前若 `git status` 有未提交改动，子代理须停下报告，不擅自混入。
