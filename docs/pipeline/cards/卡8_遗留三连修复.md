# 卡 #8（失物招领）：安检遗留三连修复 + 卫生扫尾（用户授权"修到底"）
- 背景：卡#3 安检报告记录的 3 条遗留 + 卡#2 遗留卫生项 + 卡#6 发现的 README 数字出入。
- 修复清单：
  1. **LoginView.vue:246 dev_code 兜底 '123456'（疑似真雷，优先核实）**：前端验证码输入框疑似有硬编码兜底 `'123456'`——若生产环境（SHOW_SMS_CODE=False）代码路径走到兜底自动填码，等于任何人可用 123456 注册。先读 LoginView.vue 相关逻辑：兜底代码是给"演示模式"用的还是无条件兜底？修法=删除硬编码兜底，验证码一律以用户实际输入为准（SHOW_SMS_CODE 模式下由后端返回的 dev_code 展示，不用前端常量）；
  2. **/users/me 全量自查出口**：按"本人自查全量、他人视角脱敏"惯例，/users/me 应返回本人完整手机号等；读现状（可能走了脱敏序列化器），修为本人端点返回全量（仅本人 token 可达，无越权面）；公开列表/详情继续脱敏不动；
  3. **--admin-pwd 必填**：定位该 CLI 参数（scripts/seed.py 或管理命令），有默认值改必填（缺失时报错并给生成随机密码的提示语）；
  4. **删 tests/test_zz_diag.py**（自述"验证完毕即删"的临时诊断文件，卡#2 遗留裁决项——用户已授权修到底，删除）；
  5. **README 用例数修正**：README 称"395 个用例"，实测 collected 399（397 passed + 2 skipped）——改为准确表述；
  6. CHANGELOG 记档。
- 验收（必须实际执行并贴完整输出）：
  1. 兜底修复验证：SHOW_SMS_CODE=False 下注册流程验证码框为空、无 123456 填充（代码走查贴关键行+grep `'123456'` 前端清零）；
  2. /users/me 用现有测试回归 + 若无用例补一条"本人全量/他人脱敏"断言；
  3. --admin-pwd 缺失时命令报错退出（贴输出）；
  4. `pytest` 全量 0 failed（**串行单进程**贴统计+EXIT）；
  5. py_compile；git status 仅限清单；禁止 git commit/add；命令逐个跑；失败 3 次停。
- 完成后输出：①改动清单 ②验收输出 ③三句话总结。
