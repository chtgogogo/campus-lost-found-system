# 卡 #1（失物招领）：代码侧收尾——LICENSE + CORS 收敛 + 杂物清理
- 目标：补 LICENSE、收敛 CORS、清理仓库杂物，让交付面整洁（用户已拍板"只做代码侧收尾，暂不上线"）。
- 涉及文件：
  - 新建 `LICENSE`（MIT，版权行 `Copyright (c) 2026 CaoHT`）
  - `app/main.py`（CORSMiddleware 收敛）
  - **清理清单（逐项先核实再处理）**：
    - 删除：`_dbg_v13.db`、`nul`、`3\`（先 `ls -la` 确认内容）、`_ccache_backup\`
    - 归档到 `docs/attic/`（mv 不删）：`deliverables/paper-figs/_scan_*.py`、`_fix_*.py` 等临时脚本
    - **保留不动**：`dev.db`（开发库）、`uploads\`、`.env`、`weights\`/`models\`
- 前置依赖：无
- 实现要点：
  1. CORS：`allow_origins=["*"]` → `["http://localhost:5173","http://127.0.0.1:5173"]`（先核实 `web/` 的 vite 配置实际端口与生产域名占位注释）；README 里"生产按域名收敛"的自标注随之兑现；
  2. 清理前 `git status`：若有未提交改动，**停下报告主会话**，不擅自混入；
  3. LICENSE 用标准 MIT 英文全文（开源惯例，不加中文改写）。
- 验收（怎么算做完）：
  - `pytest`（README 给出的测试命令）全绿，退出码 0，贴末尾统计行；
  - 后端可启动 `GET /health` 200；
  - `grep -n '"\*"' app/main.py` 无 CORS 命中；LICENSE 存在且为 MIT；
  - 清理清单逐项打勾，`git status` 变更仅限清单内。
- 禁止：不动 app/ 业务代码（CORS 一处除外）；不动数据库 schema 与迁移；不动 .git 历史（不做 reset/rebase）。
- 完成后动作：贴 pytest 统计行与 /health 结果；更新本台账卡#1 行；返回 3 行以内摘要。
