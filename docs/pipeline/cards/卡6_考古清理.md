# 卡 #6（失物招领）：考古清理——废弃权重/旧公式/配置文档三方对齐（安检 P2 卫生）
- 背景（外部批评"AI 尸体现场"）：①配置里同时躺**五套打分权重**全标 [deprecated] 没人敢删；②match_service.py 约 300 行已下线的旧公式留在生产代码；③`.env.example` 教用户配的参数一半不存在或语义已变——照 README+example 配出来的行为与文档宣称的不是同一个版本。git 里都有历史，旧代码不需要躺在生产里陪葬。
- 涉及文件：配置文件（deprecated 权重所在处，先 grep `deprecated|废弃|旧版|legacy` 定位）、`app/services/match_service.py`（旧公式删除）、`.env.example`（对照 `app/core/config.py` 逐项重写）、`README.md`（算法宣称口径核对）、`CHANGELOG.md`。
- 实现要点：
  1. **先盘点再删**：grep 全部 deprecated 标记与旧公式段落，列清单（所在文件:行号区间+一句话说明），确认每一项"当前代码零引用"（grep 调用点）后才删；有引用的列出对照报告，不硬删；
  2. match_service 旧公式删除以"函数级"为界：整函数下线才删，函数内混用新旧的只删死分支；
  3. `.env.example` 与 `app/core/config.py` 的 Settings 字段**逐项对照**：example 里 config 没有的删、config 里用户可调的对外项补全（含卡#3 新增的 SHOW_SMS_CODE/SEED_DEMO/REQUEST_BODY_MAX_MB/JWT_SECRET 等），全部占位符无真实值；
  4. README 算法宣称 vs match_service 现状核对：宣称的维度/权重与代码一致吗？不一致处以代码为准改 README，差异列出；
  5. CHANGELOG 记档（删了什么/为什么安全/验证方式）。
- 验收（必须实际执行并贴完整输出）：
  1. 盘点清单落卡文件（删了什么/留下什么+原因）；
  2. `pytest` 全量 0 failed（**串行单进程**，贴统计+EXIT）——行为不变是最好的"删对了"证明；
  3. `grep -rn "deprecated" app/ config 相关文件` 清零（或仅剩合理残留逐条说明）；
  4. `.env.example` 每一行都能在 config.py 找到对应字段（贴对照表）；
  5. py_compile 通过；git status 变更仅限清单；禁止 git commit/add。
- 禁止：不动匹配引擎现行算法；不做"顺手重构"；禁止 git 操作；命令逐个跑；同一操作失败 3 次停下报告。
- 完成后输出：①盘点清单 ②验收输出 ③三句话总结。

---

## 执行结果（2026-09-23）

### ① 盘点清单（先盘点、逐项 grep 调用点、零引用才删）

#### A. 已删除（全仓零业务引用）

| # | 位置 | 对象 | 零引用证据（grep 范围：app/tests/scripts/migrations/evaluation/web） |
|---|------|------|------|
| A1 | `app/core/config.py`（原 72-73 行） | `HANDOVER_TTL_MIN` 字段 + [deprecated] 注释 | 全仓仅定义处 1 命中 |
| A2 | `app/core/config.py`（原 106-115 行） | flow-v2 五维+旧六维权重 `MATCH_W_PHOTO/CAT/TEXT/LOC/TIME/APP/FEAT/OTHER`（8 字段）+ v10 废弃横幅注释 | 业务代码零命中；仅 tests/test_match.py:58-66 存续断言（本身即 [deprecated] 墓碑，随字段一并删） |
| A3 | `app/core/config.py`（原 158-159 行） | `MATCH_W_TAG`（v4 containment 权重） | 全仓仅定义处 1 命中 |
| A4 | `app/core/config.py`（原 161-165 行） | v2 旧权重 `MATCH_W1/W2/W3/W4`（4 字段） | 业务代码零命中；仅 tests/test_match.py:53-56 存续断言（随删） |
| A5 | `app/core/config.py`（原 99-105 行） | flow-v2 旧公式注释块（五维公式/「其他」特殊路径描述） | 描述对象已整体下线，注释改写为现行 v2 七维口径段头 |
| A6 | `app/services/match_service.py`（原 862-886 行） | `photo_sim_factor` / `photo_sim_factor_with_bytes` 整函数 | 全仓仅定义处+模块 docstring 提及，零调用 |
| A7 | `app/services/match_service.py`（原 57、63 行） | `clip_service.image_similarity`、`PerceptualHash` 两个 import | 仅被 A6 两函数使用；CLIP 实际消费方 clip_reorder.py 自行 import，不受影响 |
| A8 | `app/services/match_service.py`（原 987-990 行） | `tag_jaccard_factor` 整函数 | 全仓仅定义处+keyword_jaccard_factor docstring 提及，零调用 |
| A9 | `app/services/match_service.py`（原 32 行） | 模块 docstring "photo_sim_factor* 保留为 deprecated 工具方法" | 描述与删除后现实不符，改写 |
| A10 | `tests/test_match.py`（原 53-66 行） | 14 行已删字段存续断言；用例更名 `test_weights_and_threshold_config`→`test_threshold_config` | 断言对象为 A2/A4 字段默认值，非行为测试 |
| A11 | `.env.example` | `MATCH_W1..W4`、`TIME_DECAY_TAU_DAYS`、`HANDOVER_TTL_MIN`、`DEFAULT_REGION_CODE` | 前三者已下线；`DEFAULT_REGION_CODE` 为幽灵项：config.py 无此字段，Settings `extra="ignore"` 静默吞掉、代码零读取 |

删除量合计：config 14 字段 + 注释块、match_service 3 函数（约 50 行）+ 2 import、测试 14 行断言、example 6 行死参数。**现行匹配算法（v2 七维 `_evaluate`/`score_detail`/归一化）一行未动。**

#### B. 保留（有引用，逐条说明；grep 证据见执行日志）

| # | 对象 | 引用方 | 保留原因 |
|---|------|--------|----------|
| B1 | `config.TIME_DECAY_TAU_DAYS` | `match_service.time_decay_factor`（match_service.py:834） | 链条：time_decay_factor 被 tests/test_match.py 行为断言引用 → 方法保留 → 字段随之保留；注释已改 legacy 说明 |
| B2 | `MatchService.time_decay_factor` | tests/test_match.py:57,63（identical/far 两用例） | 卡规"有引用不硬删" |
| B3 | `MatchService.tag_containment_factor` | tests/test_match.py（3 个行为用例） | 同上 |
| B4 | `MatchService.color_conflict` | tests/test_match.py::test_color_conflict | 同上 |
| B5 | `MatchService.location_hit_factor` | tests/test_match.py::test_location_hit_factor | 同上 |
| B6 | `MatchService.keyword_jaccard_factor` | tests/test_match.py::test_keyword_jaccard_factor | 同上 |
| B7 | `MatchService.category_hit` | tests/test_match.py::test_category_hit_factor | 同上 |
| B8 | `MatchService.text_match_rate` | tests/test_flow_v2.py:360,361,410 | 同上 |
| B9 | `MatchService.appearance_factor / feature_factor / location_factor` | tests/test_match.py、test_flow_v2.py、test_v11_enhancements.py、test_v8_qa_independent.py | 同上 |
| B10 | `schemas/match.py` appearance/feature 字段 + `score_detail` 旧键映射（photo/category/text/text_match_rate/appearance/feature/location） | `build_match_outs`→`MatchOut.from_model`（活跃 API 出参） | R2 §7.1 老前端 JSON 消费者契约，删除即断前端；属活契约非尸体 |

**deprecated 残留 grep（`grep -rni deprecated app/`）= 8 行，全部合理**：B10 契约注释 5 行（schemas/match.py:74-75、match_service.py:1085/1088/1089）+ B4/B5/B6 保留方法 docstring 3 行（match_service.py:983/996/1003）。测试文件内的 deprecated 字样均属 B2-B9 保留项说明，不在 app/ 清零范围。

### ② .env.example ↔ config.py 逐项对照表（重写后）

| example 行 | config.py 字段 | 关系 |
|---|---|---|
| APP_NAME / API_V1_PREFIX / DEBUG | 同名 str/str/bool | 一致 |
| SHOW_SMS_CODE | SHOW_SMS_CODE: bool | 卡#3 新增，已在 |
| RATE_LIMIT_ENABLED | RATE_LIMIT_ENABLED: bool | 一致 |
| SEED_DEMO | SEED_DEMO: bool | 卡#3 新增，已在 |
| DATABASE_URL / DB_ECHO | 同名 str/bool | 一致（口令占位） |
| REDIS_ENABLED / REDIS_URL | 同名 bool/str | 一致 |
| JWT_SECRET | JWT_SECRET: str | 卡#3 新增，已在；示例值在启动黑名单，照抄即 fail-fast（设计如此） |
| JWT_ALGORITHM / ACCESS_TOKEN_EXPIRE_MIN / REFRESH_TOKEN_EXPIRE_DAYS | 同名 | 一致 |
| ADMIN_APPLY_CODE | ADMIN_APPLY_CODE: str | 修正：示例值→留空（=通道禁用，与 config 默认一致） |
| ADMIN_RETENTION_DAYS | ADMIN_RETENTION_DAYS: int | 新补 |
| YOLO_DEVICE / YOLO_MODEL_DIR / YOLO_FALLBACK_CATEGORY_ID / YOLO_WORLD_MODEL | 同名 | 一致 |
| YOLO_COCO_MODEL | YOLO_COCO_MODEL: str | 值修正 yolov8n.pt→best.pt（config 默认为用户训练 11 类模型） |
| YOLO_CONF_THRESHOLD | YOLO_CONF_THRESHOLD: float | 值修正 0.25→0.12（config 默认） |
| HANDOVER_TTL_SEC | HANDOVER_TTL_SEC: int | 替换已删 HANDOVER_TTL_MIN（现行双码模型 10 秒） |
| SMS_RATE_LIMIT_PER_MIN / SMS_RESEND_INTERVAL_SEC | 同名 | 一致 |
| RATE_LIMIT_AUTH/PUBLISH/PREVIEW_PER_MIN | 同名 ×3 | 新补 |
| REQUEST_BODY_MAX_MB | REQUEST_BODY_MAX_MB: int | 卡#3 新增，已在 |
| MATCH_THRESHOLD / MATCH_LOW_SCORE / MATCH_TOP_N / MATCH_SUSPECT_MAX | 同名 | 前者一致，后三者新补 |
| MATCH_NORMALIZE / MATCH_NORM_MIN_WEIGHT / MATCH_TIME_TAU_DAYS | 同名 | 新补（现行 v2 归一化口径） |
| MATCH_W2_PHOTO_CAT/QTY/COLOR/STATE/PLACE/KEYWORD/TIME | MATCH_W2_* ×7 | 新补（现行七维权重） |
| IMG_MAX_COUNT / IMG_MAX_SIZE_MB / IMG_STORAGE / #UPLOAD_DIR | 同名 | 前三者一致，UPLOAD_DIR 注释项新补 |
| IM_RETENTION_DAYS | IM_RETENTION_DAYS: int | 值修正 7→30（config v3 Q7 默认） |
| AUDIT_RETENTION_DAYS | AUDIT_RETENTION_DAYS: int | 一致 |
| IM_POLL_INTERVAL_MS | IM_POLL_INTERVAL_MS: int | 新补 |
| DEFAULT_PAGE_SIZE / MAX_PAGE_SIZE | 同名 | 一致 |

未列入 example 的 config 字段：`OTHER_CATEGORY_NAME`（业务枚举名）、`MATCH_TIME_TAU_DAYS` 以外的内部常量、`BASE_DIR` 派生路径默认值（`YOLO_MODEL_DIR`/`UPLOAD_DIR` 绝对路径默认）——均不宜运维经 .env 调整。example 现共 44 个有效键 + 1 注释键，**每一行都能在 config.py 找到同名字段**。

### ③ README 算法宣称核对结论

- 七维权重表（类目20/颜色20/数量15/地点15/状态10/关键词10/时间10）＝ `MATCH_W2_*`：**一致，不动**；
- 归一化 k=100/max(已填,50)、疑似阈值 80、四类强冲突、候选池 50：**与 config/_evaluate 一致，不动**；
- **修正 3 处**（以 clip_reorder.py + routers/match.py 次排序代码为准）：CLIP 不进打分——首段、模块表、流程步骤改为"发布后台精排、仅同分打破平局、不改匹配分"。
- 备注（范围外未改）：README"395 个用例"与实测 397 passed+2 skipped 有出入，非算法宣称，留主程定夺。

### ④ 验收输出（实际执行）

1. py_compile（config.py / match_service.py / test_match.py）：`PY_COMPILE_OK`；
2. `pytest` 全量串行单进程：`397 passed, 2 skipped, 584 warnings in 458.46s (0:07:38)`，`PYTEST_EXIT=0`，**0 failed**，与卡#5 基线（397 passed, 2 skipped）完全一致——行为不变即"删对了"；
3. deprecated grep 残留 8 行逐条说明见上②；
4. `.env.example` 对照表见上②；
5. git status 变更仅限本卡清单（config.py / match_service.py / test_match.py / .env.example / README.md / CHANGELOG.md / 本卡文件）；全程零 git add/commit。

