# 卡 #7（失物招领）：同步推理移出事件循环（安检 P3 架构）
- 背景（外部批评，两项目同犯）：`async def` 路由跑在单线程事件循环上，内部同步执行秒级任务（本项目=YOLO/CLIP 视觉推理）会阻塞整个循环——期间所有请求包括登录都在排队。
- 任务：①grep 全部 `async def` 路由，逐个判定内部是否有同步重活（模型推理/大文件 IO/秒级外部调用），列清单；②命中的路由改为 `def`（FastAPI 自动线程池）或 `await asyncio.to_thread(重活)`——按改动面最小选择；③纯轻量 IO 的 async 路由不动；④若模型加载/推理入口在 service 且被多处 async 调用，优先在调用点 to_thread。
- 验收（必须实际执行并贴完整输出）：
  1. 清单：每个 async 路由→有无重活→处理方式（改 def/to_thread/无需）；
  2. 视觉接口回归：上传图片走一遍识别路径（现有测试覆盖即可，贴相关 pytest 输出）；
  3. `pytest` 全量 0 failed（**串行单进程**贴统计+EXIT）；py_compile；git status 仅限清单。
- 禁止：不改推理模型与阈值；不动业务逻辑；禁止 git 操作；命令逐个跑；失败 3 次停。
- 完成后输出：①清单+处理方式 ②验收输出 ③三句话总结。
